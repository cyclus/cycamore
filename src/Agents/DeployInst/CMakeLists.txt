SET(LIB_ROOT "DeployInst")
SET(SRC_ROOT "deploy_inst") 

USE_CYCLUS(${LIB_ROOT} ${SRC_ROOT})

SET(
  CYCAMORE_LIBRARIES ${CYCAMORE_LIBRARIES}
  ${LIB_ROOT}_LIB
  PARENT_SCOPE
  )

SET(
  TestSource ${TestSource}
  ${${LIB_ROOT}_TEST_CC}
  PARENT_SCOPE
  )