SET(LIB_ROOT "NullInst")
SET(SRC_ROOT "null_inst") 

USE_CYCLUS(${LIB_ROOT} ${SRC_ROOT})

SET(
  CYCAMORE_LIBRARIES ${CYCAMORE_LIBRARIES}
  ${LIB_ROOT}_LIB
  PARENT_SCOPE
  )

SET(
  TestSource ${TestSource}
  ${${LIB_ROOT}_TEST_CC}
  PARENT_SCOPE
  )